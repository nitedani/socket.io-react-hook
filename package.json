{
  "name": "socket.io-react-hook",
  "version": "2.2.3",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "release": "pnpm build && test-e2e && bumpp package.json packages/*/package.json examples/*/package.json && pnpm -r publish --access=public",
    "test": "test-e2e"
  },
  "devDependencies": {
    "@brillout/test-e2e": "^0.1.22",
    "bumpp": "^8.2.1",
    "turbo": "^1.4.6"
  },
  "engines": {},
  "dependencies": {
    "kill-port": "^2.0.1"
  }
}
